"""테스트 공용 상수.

차단 목록을 여기 두는 이유가 있다. `app/` 안에 두면 소스에 그 이름이 다시 등장하고,
'쓰지 않는 제공자가 소스에 없다'를 검사하는 테스트가 스스로 깨진다.
검사 대상이 아닌 곳(tests/)에 둬야 검사가 성립한다.
"""
from __future__ import annotations

# 무료 티어로 쓸 수 있는 경로 제공자만 붙인다. 유료를 섞으면 배포에 고정비가 생기고,
# 키가 없는 심사·데모 환경에서는 그 구간이 통째로 비어 일정이 성립하지 않는다.
# 아래는 그래서 쓰지 않기로 한 제공자 목록이다 — 기능 설명이 아니라 차단 목록이다.
NON_FREE_ROUTING_PROVIDERS = ("tmap",)

# 실제로 배선돼 있어야 하는 제공자. '유료가 없다'만 검사하면 전부 지워도 통과한다.
FREE_ROUTING_PROVIDERS = ("naver", "openrouteservice", "odsay")
